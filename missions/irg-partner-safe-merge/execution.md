# Execution Log — IRG Partner Safe Merge

- 2026-07-20 Documentación: tras Review `APPROVED` y Validación final `passed`, se añadieron el README operativo del addon, `missions/irg-partner-safe-merge/CHANGELOG.md` y la entrada reutilizable de knowledge sobre inventario relacional cerrado, modelos sin `id` y reintentos de concurrencia de Odoo.
- 2026-07-20 Checks de Documentación: se comprobó el alcance contra manifest, modelos, wizard, vistas, micro-spec y `verification.json`; no se modificaron código, tests, seguridad, vistas, manifest ni configuración de runtime. Se verificó Markdown y enlaces relativos (no se añadieron enlaces relativos), ausencia de contradicciones con el gate `passed` y estado Git final. Se eliminó `.superpowers/sdd/progress.md`, que estaba obsoleto y marcaba la misión como pendiente.

- 2026-07-20: misión creada antes de cambios funcionales.
- 2026-07-20: base `cb3fb84603954925e165f32c5ad8a4e4e5844a3b`; rama `codex/irg-partner-safe-merge`; worktree aislado en `C:\tmp\Odoo16iRG-irg-partner-safe-merge`.
- 2026-07-20: tier `complex`; se requiere Security Advisor por concurrencia e integridad de datos.
- 2026-07-20: `docker-compose.local.yml` no está versionado y no existe en el worktree; la validación usará el compose del checkout principal más un overlay que monte este worktree y restaurará el servicio al finalizar.
- 2026-07-20: Security Advisor emitió `[NO]`: transferencia dinámica amplia, frontera ORM/SQL, locks, permisos, semántica de relaciones, rollback y micro-spec insuficientes.
- 2026-07-20: plan enmendado con allowlist cerrada, ORM por defecto, bloqueo determinista, idempotencia, auditoría inmutable, políticas explícitas y bloqueo de contabilidad/relaciones desconocidas. Se solicita nueva revisión antes de implementación.
- 2026-07-20: segundo dictamen `[NO]` limitado a tres ajustes: gradebooks related, identidad externa y protección RPC del marcador/origen.
- 2026-07-20: micro-spec corregida: gradebooks related se recalculan; `ir.model.data` se conserva; marcador solo por servicio `su`; reactivación/eliminación bloqueadas y cubiertas por pruebas RPC.
- 2026-07-20: Security Advisor emitió `[YES]`: las tres correcciones requeridas quedaron incorporadas y el diseño puede pasar a Implementación/TDD.
- 2026-07-20: el helper Bash `task-brief` no pudo ejecutarse porque Windows no tiene una distribución WSL instalada; se generó manualmente un brief equivalente desde el plan, sin cambiar alcance ni criterios.
- 2026-07-20 Implementación/TDD: se crearon primero `tests/test_partner_safe_merge.py` (suite Odoo) y `tests/test_static_contract.py` (contrato ejecutable sin Odoo), antes de cualquier archivo de producción.
- 2026-07-20 RED genuino: `C:\Users\admin\.local\bin\python3.11.exe addons-extra\extrairg\irg_partner_safe_merge\tests\test_static_contract.py` terminó con código 1: faltaban los once archivos requeridos del addon y `wizard/partner_safe_merge_wizard.py` no existía. El fallo corresponde exactamente a la funcionalidad aún no implementada.
- 2026-07-20 limitación objetiva previa a producción: `docker version` no pudo conectar con `npipe:////./pipe/docker_engine` porque el daemon Docker no está iniciado. El intento enfocado `docker compose -f C:\Users\admin\Documents\iRG\Odoo16iRG\docker-compose.local.yml run --rm odoo_local odoo --stop-after-init --test-enable --test-tags /irg_partner_safe_merge -i irg_partner_safe_merge` terminó con código 1 por la misma ausencia del daemon. La suite Odoo no puede ejecutarse en esta fase y no se considerará pasada; se usará el contrato estático, compilación y parseo como sustituto disponible.
- 2026-07-20 decisión de implementación: el motor residirá en el modelo transitorio y solo manipulará pares `modelo.campo` enumerados en constantes; el catálogo de metadatos se empleará exclusivamente para detectar y bloquear referencias no clasificadas. Locks SQL quedan limitados a IDs enteros y tablas/columnas obtenidas de la allowlist estática.
- 2026-07-20 GREEN estático: `C:\Users\admin\.local\bin\python3.11.exe addons-extra\extrairg\irg_partner_safe_merge\tests\test_static_contract.py` terminó con código 0: `Ran 4 tests ... OK`. El contrato verifica archivos obligatorios, AST de Python, parseo XML/CSV, primitivas prohibidas, allowlists y guardas.
- 2026-07-20 compilación: `C:\Users\admin\.local\bin\python3.11.exe -m compileall -q addons-extra\extrairg\irg_partner_safe_merge` terminó con código 0 y sin salida.
- 2026-07-20 lint adicional: `python3.11 -m ruff check ...` no se pudo ejecutar porque Ruff no está instalado (`No module named ruff`); no se descargaron dependencias. Se usaron compilación, AST, parseo estructural y revisión de whitespace como sustitutos disponibles.
- 2026-07-20 comprobaciones finales de implementación: `git diff --check` código 0; búsqueda de `._merge(`, `cr.commit(` y `DELETE FROM` sin coincidencias; revisión PowerShell sin whitespace final; `git status --short` confirma únicamente rutas nuevas/no versionadas de la misión, planes y addon, sin cambios a addons existentes.
- 2026-07-20 decisión de concurrencia/integridad: contactos y filas planificadas se bloquean en orden estable; el payload se recalcula después del lock y se compara con SHA-256 del preview. Los índices únicos afectados se prevalidan desde catálogo, y cualquier error ORM/SQL se deja propagar para rollback completo de la petición.
- 2026-07-20 limitación de cierre de Implementación: la suite Odoo etiquetada `irg_partner_safe_merge` quedó escrita pero no ejecutada por ausencia del daemon Docker. No se afirma GREEN de integración Odoo ni instalación del módulo; este gate debe ejecutarlo Validación con `docker-compose.local.yml` y overlay del worktree cuando Docker esté disponible.
- 2026-07-20 reentrada a Implementación tras Review: `artifacts/review.md` devolvió `CHANGES_REQUESTED` con cinco P1 y un P2. Se verificaron contra el código: inventario sin M2M, decisiones fuera del hash, contexto interno falsificable sin `su`, coherencia parcial usuario/estudiante, transient allowlisted filtrado, pruebas incompletas y colisiones semánticas solo parciales.
- 2026-07-20 RED de corrección antes de producción: se añadieron contratos para M2M/plan/su/transient/business policies y snapshots. `python3.11 tests/test_static_contract.py` terminó código 1 con dos fallos esperados: no existían `APPROVED_M2M_ALLOWLIST`/plan ligado ni `before_snapshot_json`/`after_snapshot_json`.
- 2026-07-20 corrección TDD: el inventario ahora inspecciona M2M directos y bloquea todos salvo `res.partner.category_id`; categorías incluyen filas source/master en hash y lock explícito de `res_partner_res_partner_category_rel`. `op.admission.elearning.wizard` se inventaría explícitamente pese a ser transient y entra en locks/transferencia ORM.
- 2026-07-20 corrección de integridad de plan: la primera preview genera conflictos; tras elecciones una segunda preview liga `decisions` y `student_user_links` al SHA-256. Confirmación bloquea wizard/líneas, contactos, filas planificadas y relación de categorías; revalida y rehasha el plan efectivo bajo lock. Escrituras internas de wizard/líneas exigen simultáneamente `env.su` y contexto privado.
- 2026-07-20 corrección de coherencia: se validan todos los estados de usuario/estudiante; si ambos existen en un contacto deben enlazarse exactamente, y si maestro y origen contienen cualquier grafo de identidad se bloquea para resolución manual. El enlace exacto queda serializado en preview/hash.
- 2026-07-20 corrección de colisiones: políticas explícitas y estado target/source hasheado/bloqueado para `op.admission` (`register/course/batch`), `appisep.gradebook.summary` (`course/batch` o admisión), `sale.order` (`company/client_order_ref` o admisión) y `sale.subscription.schedule` (`term_number/date_due`), además de políticas previas de canal/Stripe e índices únicos.
- 2026-07-20 corrección de auditoría: se añadieron snapshots JSON inmutables antes/después con identidad, estado activo/marcador, escalares, categorías, usuarios, estudiantes y enlaces estudiante-usuario; la auditoría se sigue creando al final.
- 2026-07-20 pruebas Odoo ampliadas: Camila equivalente incluye cuatro leads, dos ventas, dos admisiones, schedule, usuario/estudiante, mensaje, actividad y adjunto; blockers aislados para banco, usuarios, estudiantes, incoherencia, M2M/M2O y políticas de pago/contabilidad; colisiones por dominio; transient; hash de decisiones; snapshots; rollback por fase con grafo real y aserciones de cada mutación; idempotencia y orden canónico same/inverse.
- 2026-07-20 RED adicional de UI: contrato código 1 demostró que `conflict_ids` era readonly con `preview_ready=False`; se invirtió el modificador para permitir elección antes de preview final y bloquear después. GREEN final: `Ran 7 tests ... OK`.
- 2026-07-20 verificación estática post-corrección: `compileall -q` código 0; contrato estático 7/7; XML/CSV parseados por contrato; búsqueda de `_merge`, `cr.commit` y `DELETE FROM` sin coincidencias; whitespace pass; `git diff --check` pass; estado Git solo rutas nuevas/no versionadas, sin commit/push/PR.
- 2026-07-20 runtime rechecado: `docker version` sigue sin conectar a `npipe:////./pipe/docker_engine`. La suite Odoo ampliada y concurrencia real entre dos transacciones continúan pendientes del gate de Validación; TransactionCase cubre idempotencia secuencial, inversión y orden canónico, pero no se afirma prueba concurrente real sin runtime.
- 2026-07-20 tercera reentrada a Implementación: el re-review confirmó resueltas las correcciones de producción y devolvió cuatro refuerzos exclusivamente de pruebas: RPC con administrador real no-`su`, confirmaciones concurrentes same/inverse con cursores independientes, blocker funcional de banco y rollback escalar/follower.
- 2026-07-20 RED dirigido antes de editar la suite Odoo: se amplió `test_static_contract.py` con el contrato de esos cuatro escenarios. `C:\Users\admin\.local\bin\python3.11.exe addons-extra\extrairg\irg_partner_safe_merge\tests\test_static_contract.py` terminó código 1 (`7 passed, 1 failed`) porque faltaba `base.user_admin` y el resto de marcadores de las nuevas pruebas runtime.
- 2026-07-20 corrección de pruebas: el tampering RPC usa `base.user_admin` mediante un `api.Environment` con `su=False`; el blocker crea una fila real `res.partner.bank` enlazada al origen y ejecuta `action_preview()`; rollback conserva y verifica el `street` del maestro y un follower real, además de usuario, estudiante, lead, categoría y adjunto.
- 2026-07-20 corrección concurrente: se añadieron casos same e inverse con fixtures confirmadas, dos `registry.cursor()`/entornos no-`su`, dos `threading.Thread`, sincronización por `Event`, contención demostrada antes de liberar el primer lock, `statement_timeout`, joins acotados, verificación de una sola auditoría/marker y cleanup determinista en cursor separado. Same exige dos resultados con el mismo audit; inverse exige un resultado y `ValidationError` para el perdedor.
- 2026-07-20 GREEN dirigido: el contrato estático ampliado terminó código 0, `Ran 8 tests ... OK`. `compileall -q` terminó código 0; escaneo de primitivas prohibidas limitado a producción sin coincidencias; whitespace sin coincidencias; `git diff --check` código 0; Git mantiene solo rutas nuevas de addon/misión/planes, sin cambios a addons existentes ni commit/push/PR.
- 2026-07-20 runtime rechecado tras la tercera corrección: `docker version` vuelve a terminar código 1; el cliente existe pero el daemon sigue ausente en `npipe:////./pipe/docker_engine`. Las nuevas pruebas Odoo multitransacción están implementadas pero no se declaran ejecutadas ni pasadas; quedan para Review/Validación cuando el runtime esté disponible.
- 2026-07-20 cuarta reentrada a Implementación: la tercera revisión dejó un único P1 de test; `res.partner.bank` no demostraba el bloqueo de un asiento/pago real. No se requirió modificar producción.
- 2026-07-20 RED contable: antes de editar la suite Odoo se amplió el contrato estático para exigir `test_source_account_move_blocks_preview`, creación ORM real de `account.move` y aserción específica sobre `account.move.commercial_partner_id|partner_id`. El contrato terminó código 1 (`7 passed, 1 failed`) por ausencia del nuevo escenario.
- 2026-07-20 GREEN contable: se añadió un test aislado que reutiliza un diario general de la compañía (o crea uno determinista si no existe), crea un `account.move` de tipo `entry`, estado `draft` y `partner_id=source`, verifica esos datos y exige que `action_preview()` eleve `ValidationError` mencionando la relación contable concreta. TransactionCase revierte el fixture; banco y demás blockers no participan en el caso. Contrato estático final `Ran 8 tests ... OK`; `compileall -q` código 0; scan de primitivas prohibidas en producción sin coincidencias. Sin cambios de producción, commit, push ni PR.
- 2026-07-20 Validación independiente: se releyeron `AGENTS.md`, plan, micro-spec, execution, brief/report, knowledge/workflow y `artifacts/review.md` hasta su sign-off final `APPROVED`; no se editó código de producción.
- 2026-07-20 Validación estática fresca: el contrato ejecutable terminó `Ran 8 tests in 0.021s ... OK` (exit 0); 10 fuentes Python compilaron en memoria (exit 0); XML/CSV quedan cubiertos por el contrato; escaneo de `._merge(`, `cr.commit(` y `DELETE FROM` fuera de tests sin coincidencias; 14 archivos sin whitespace final; `git diff --check` exit 0.
- 2026-07-20 Alcance Git independiente: HEAD/base `cb3fb84603954925e165f32c5ad8a4e4e5844a3b`, sin cambios tracked/staged y producto limitado al addon nuevo. El resto son planes/artefactos de misión; `.superpowers/sdd/progress.md` está obsoleto (`in_progress/pending assignment`) y se deja anotado para Documentación.
- 2026-07-20 Runtime obligatorio: `docker version` exit 1. Cliente 29.4.3 disponible, pero no existe `npipe:////./pipe/docker_engine`; también aparece advertencia de acceso denegado a `C:\Users\admin\.docker\config.json`. No se crearon overlay, contenedores, volúmenes, redes ni base aislada; por seguridad no se ejecutó compose sin el overlay del worktree. Instalación, suite `/irg_partner_safe_merge` y concurrencia PostgreSQL quedan sin ejecutar y no se declaran pasadas.
- 2026-07-20 Cleanup/restauración: al no crearse recursos no hubo nada que limpiar ni servicio que restaurar. El compose canónico conserva SHA256 `11F6D052BE34E313A42D733A69E7E73D125A107CAD9B5C2CE5798FA2ED4EA49C` y `etc/odoo.local.conf` conserva `8A00253579D8E009F8AF91091C0E0034F78B1208BCAB9F9CEC6A71F3AFA1B299`; ningún servicio compartido quedó apuntando al worktree.
- 2026-07-20 Gate de Validación: `missions/irg-partner-safe-merge/verification.json` emitido con `status: failed` porque el runtime Odoo es obligatorio y el daemon Docker no está disponible. Se reabre Implementación/Validación cuando el runtime pueda ejecutarse; no hubo commit, push ni PR.
- 2026-07-20 Revalidación final del validador: contrato estático fresco `Ran 8 tests in 0.020s ... OK`; JSON parsea con 9 checks y cuatro evidencias existentes; `git diff --check` exit 0; hashes canónicos sin cambios. `docker version` fuera del sandbox usa contexto `desktop-linux` y también falla (exit 1) porque no existe `npipe:////./pipe/dockerDesktopLinuxEngine`. El gate permanece `failed`; no se modificó el contexto ni se creó recurso runtime alguno.
- 2026-07-20 Nueva Validación independiente solicitada tras fast-forward: se releyó el `AGENTS.md` vigente, que distingue Review de código, Validación y Documentación. HEAD/base actual `0ea0c8fdb0ab18bfcbbfc76160920c3103fd23b7`; el addon sigue sin cambios tracked y no se editó código de producción.
- 2026-07-20 Runtime recuperado: `docker version` exit 0 con cliente/servidor 29.4.3, Docker Desktop 4.74.0 y contexto `desktop-linux`. `odoo_local` y `pgodoo_local` ya estaban `Up` antes de validar; se registró ese preestado para restauración.
- 2026-07-20 Aislamiento: overlay temporal creado con `apply_patch`, montando exclusivamente `C:/tmp/Odoo16iRG-irg-partner-safe-merge/addons-extra` en `/mnt/extra-addons` y el volumen único `irg_psm_val_0ea0c8f_20260720_01_filestore` en `/var/lib/odoo`. `docker compose ... config` exit 0 confirmó ambos targets. La DB única `irg_psm_val_0ea0c8f_20260720_01` y el volumen no existían antes.
- 2026-07-20 Instalación/suite obligatoria: `docker compose -f <compose-canónico> -f <overlay-worktree> run --rm --no-deps odoo_local odoo --stop-after-init --test-enable --test-tags /irg_partner_safe_merge -i irg_partner_safe_merge -d irg_psm_val_0ea0c8f_20260720_01` instaló el grafo y alcanzó los post-tests del addon, pero terminó con `0 failed, 19 error(s) of 30 tests`. Una repetición enfocada idéntica mediante `-u irg_partner_safe_merge --log-level=critical` confirmó exit 1 en 28.6347007 s.
- 2026-07-20 Fallo de producción hallado por runtime: 14 tests que invocan preview fallan en `wizard/partner_safe_merge_wizard.py:487`; `_relation_inventory()` descubre `hr.employee.base` y ejecuta `model.search(..., order="id")`, pero ese modelo no tiene campo `id`. Error exacto: `ValueError: Invalid field 'id' on model 'hr.employee.base'`.
- 2026-07-20 Concurrencia: `test_concurrent_same_confirmation_is_idempotent` y `test_concurrent_inverse_confirmation_blocks_loser` fallan durante `action_preview()` por el error anterior, antes de arrancar threads/transacciones contendientes. La concurrencia real queda no validada y el check se registra como `fail`, no como skip.
- 2026-07-20 Fallos adicionales de fixtures: `test_admission_business_collision_blocks` y `test_gradebook_business_collision_blocks` crean `op.admission.register` sin contador positivo y reciben `ValidationError: No of Admission should be positive!`; Camila, colisión de ventas y colisión de schedule crean ventas batch y el override instalado `isep_form_data` falla con `ValueError: Expected singleton: res.partner(...)`. Son cinco errores de suite distintos del defecto de inventario.
- 2026-07-20 Estático fresco sobre base actual: contrato `Ran 8 tests in 0.020s ... OK`; 10 Python compilados en memoria; XML/CSV cubiertos; primitivas prohibidas sin coincidencias; 14 archivos sin whitespace final; `git diff --check` exit 0; 0 cambios tracked y 0 staged.
- 2026-07-20 Cleanup/restauración obligatorio: DB única eliminada con `dropdb --force` exit 0; volumen único eliminado exit 0; run containers ausentes; overlay eliminado mediante `apply_patch`. Consulta post-cleanup de DB vacía y `volume inspect` devuelve ausencia esperada. Los servicios canónicos continúan `Up` con los mismos puertos; hashes compose/config permanecen `11F6D052...49C` y `8A002535...299`.
- 2026-07-20 Gate actualizado: `verification.json` conserva `status: failed`, ahora por fallos runtime reproducibles del addon/suite y no por falta de daemon. Evidencia reemplazada por la ejecución sobre base `0ea0c8fdb`; no hubo commit, push ni PR.
- 2026-07-20 quinta reentrada a Implementación por fallos runtime: se añadió primero `test_preview_skips_abstract_partner_model_without_id`. RED real sobre la base aislada `irg_psm_impl_0ea0c8f_20260720_02_red`: `0 failed, 1 error` con `ValueError: Invalid field 'id' on model 'hr.employee.base'`.
- 2026-07-20 corrección de producción acotada: el inventario omite únicamente modelos de registro sin campo ORM `id`; no usa `_auto=False`, por lo que conserva la inspección de vistas SQL persistentes. El guard se aplica a many2one, many2many, reference y referencias polimórficas. GREEN dirigido: `0 failed, 0 errors` de 1 test.
- 2026-07-20 fixtures runtime corregidos: `op.admission.register.min_count=1`; ventas creadas como singletons por compatibilidad con el override instalado `isep_form_data`; usuarios sintéticos sin suscripción automática a canales; tarjetas `res.card` generadas incondicionalmente por el override de venta se retiran solo del fixture Camila antes del preview. Ninguna política de producción se amplió para transferir canales o tarjetas bloqueadas.
- 2026-07-20 M2M inversa: `res.partner.category.partner_ids` se declaró como la vista inversa aprobada de la misma relación semántica `res.partner.category_id`; continúa bloqueada cualquier otra M2M directa desconocida.
- 2026-07-20 concurrencia runtime: las dos peticiones de prueba atraviesan `odoo.service.model.retrying`, el mismo límite oficial de Odoo. En same e inverse PostgreSQL produjo `SerializationFailure` real bajo `FOR UPDATE`; Odoo reintentó, same devolvió la misma auditoría e inverse terminó en `ValidationError`, con una sola auditoría en ambos casos.
- 2026-07-20 suite completa GREEN tras upgrade: `31 post-tests in 7.96s, 15325 queries`; resultado exacto `0 failed, 0 error(s) of 31 tests`, exit 0.
- 2026-07-20 instalación limpia GREEN en la base nueva `irg_psm_impl_0ea0c8f_20260720_03_green`: `31 post-tests in 9.92s, 15324 queries`; resultado exacto `0 failed, 0 error(s) of 31 tests`, exit 0.
- 2026-07-20 estático de Implementación: contrato `Ran 8 tests in 0.025s ... OK`; `compileall -q` exit 0; primitivas prohibidas sin coincidencias; `git diff --check` exit 0.
- 2026-07-20 cleanup/restauración de Implementación: ambas DB eliminadas, volumen `irg_psm_impl_0ea0c8f_20260720_02_filestore` eliminado, run containers y overlay ausentes. `odoo_local`/`pgodoo_local` siguen `Up`; hashes canónicos permanecen `11F6D052...49C` y `8A002535...299`.
- 2026-07-20 `verification.json` y `artifacts/implementation-runtime.txt` se actualizan solo como evidencia del codificador. El estado permanece `failed` hasta que un validador independiente repita los checks y reemplace el gate final. Sin commit, push ni PR.
- 2026-07-20 Validación final independiente de la versión corregida: se releyó el `AGENTS.md` vigente y el re-review `APPROVED`; no se confió en la evidencia del codificador ni se editó código de producción. HEAD/base `0ea0c8fdb0ab18bfcbbfc76160920c3103fd23b7`.
- 2026-07-20 Preestado runtime final: `docker version` exit 0, cliente/servidor 29.4.3, Docker Desktop 4.74.0, contexto `desktop-linux`; `odoo_local` y `pgodoo_local` ya estaban `Up`. DB `irg_psm_val_0ea0c8f_20260720_02` ausente y volumen `irg_psm_val_0ea0c8f_20260720_02_filestore` ausente antes de empezar.
- 2026-07-20 Overlay final temporal: `docker compose ... config` exit 0 confirmó `C:/tmp/Odoo16iRG-irg-partner-safe-merge/addons-extra -> /mnt/extra-addons` y filestore único `_02 -> /var/lib/odoo`; no se montó el checkout principal sucio.
- 2026-07-20 Instalación limpia y suite completa: `docker compose -f <compose-canónico> -f <overlay> run --rm --no-deps odoo_local odoo --stop-after-init --test-enable --test-tags /irg_partner_safe_merge -i irg_partner_safe_merge -d irg_psm_val_0ea0c8f_20260720_02 ...` terminó exit 0. Estado previo al cleanup: `irg_partner_safe_merge:installed`. Totales exactos: stats `33 tests 9.37s 15324 queries`; result `0 failed, 0 error(s) of 31 tests`.
- 2026-07-20 Concurrencia final: inverse produjo `SerializationFailure` sobre partners `(113,114)` y retry de Odoo con 4 intentos restantes; same produjo la misma contención sobre `(115,116)` y retry. Ambos tests terminaron dentro de la suite 31/31 GREEN, demostrando contención PostgreSQL real y recuperación transaccional.
- 2026-07-20 Diagnósticos no bloqueantes del entorno: módulos preexistentes registraron intentos fallidos de imponer NOT NULL sobre `sign_template.sale_id`, `op_course.lang` y `product_template.course_type`, y metadata duplicada en `isep_sale_subscription_custom`. No abortaron la instalación: el addon quedó `installed`, el proceso salió 0 y la suite enfocada terminó sin fallos/errores. Ninguna ruta del addon nuevo originó esos logs.
- 2026-07-20 Estático final fresco: contrato `Ran 8 tests in 0.026s ... OK` exit 0; 10 Python compilados en memoria; XML/CSV cubiertos; primitivas prohibidas sin coincidencias; 14 archivos sin whitespace final; `git diff --check` exit 0; 0 tracked y 0 staged.
- 2026-07-20 Cleanup/restauración final: DB `_02` eliminada con exit 0, volumen `_02_filestore` eliminado con exit 0, run container ausente y overlay eliminado mediante `apply_patch`. Consulta post-cleanup vacía y volume inspect confirma ausencia. Servicios canónicos continúan `Up`; hashes compose/config permanecen `11F6D052...49C` y `8A002535...299`.
- 2026-07-20 Gate final de Validación: `verification.json` reemplazado con `status: passed`, 10 checks `pass`, 0 `fail`, 0 `skipped` y cuatro evidencias existentes. No hubo commit, push ni PR.
