# Execution log — irg_tfm_convocatorias

- 2026-09-04: misión `full`, tier `complex`; plan y micro-spec creados antes de código funcional.
- 2026-09-04: worktree aislado `codex/irg-tfm-convocatorias` en `C:/Users/admin/AppData/Local/Temp/Odoo16iRG-worktrees/irg-tfm-convocatorias`, base `928c2974b` (`origin/Dev_iRG`).
- 2026-09-04: baseline Git limpio. `docker-compose.local.yml` existe solo en el checkout principal y Docker no estaba disponible durante el primer intento; se reserva overlay/run para validación.
- Ruling: se adapta Subagent-Driven Development sin commits intermedios porque AGENTS.md exige autorización explícita para commit; las revisiones usarán diffs y reportes por tarea.
- 2026-09-04: Security Advisor inicial `[NO]`; exigió neutralización total legacy, propiedad fail-closed, POST+CSRF, validación de formato real, inmutabilidad/excepciones, serialización, procedencia de membresías, predicado eLearning preciso, supresión contextual del correo y `ondelete=restrict`. Plan y micro-spec enmendados; pendiente segunda revisión.
- 2026-09-04: Security Advisor segunda ronda `[NO]`; se concretaron override sin `super()` y dependencias de carga para MyCampus, comprobación interna `base.group_user`, DOC OLE Word real y reglas no contradictorias para memberships activas/archivadas ajenas. Pendiente tercera revisión.
- 2026-09-04: Security Advisor tercera ronda `[YES]`; implementación autorizada con todos los controles del diseño en vigor.
- 2026-09-04: Task 1 implementó catálogo, parser/cortes, activación inmediata+cron acotado, constraint/preflight, supresión contextual de email y vistas backend. Review detectó 3 Important + 1 Minor; fix round 1 corrigió los cuatro y re-review aprobó. Runtime Odoo pendiente.
- 2026-09-04: Task 2 implementó entregas versionadas e inmutables, validación real de archivos, MyCampus seguro y neutralización legacy. Review corrigió en ronda 1 FIB DOC, test de ventana, colisión con controlador de diplomados y dos aserciones; ronda 2 cerró un disclosure del POST de diplomados. Re-review aprobó. Runtime Odoo pendiente.
- 2026-09-04: Task 3 implementó filtrado eLearning fail-closed por convocatoria, protección de acceso directo antes del controlador heredado, QWeb combinado, canal TFM por curso y memberships con procedencia compartida. Review ronda 1 exigió ocultar cabeceras restringidas y cobertura ORM/HTTP real; la primera corrección dejó un fixture incompatible con la unicidad de matrícula. Ronda 2 usó un segundo curso sobre el mismo canal/lote y demostró preservación hasta retirar la última tesis. Re-review aprobó sin observaciones Critical/Important.
- 2026-09-04: Docker Desktop está instalado, pero el daemon no arrancó: el pipe `dockerDesktopLinuxEngine` no existe y Windows denegó iniciar `com.docker.service`. Se conserva esta limitación para la validación independiente; no se afirma ejecución de Odoo ni E2E.
- 2026-09-04: Review integral del addon abrió tres rondas de corrección. Ronda 1 cerró TOCTOU de asignación, convocatorias archivadas, fixtures sin canal y firmas PDF/DOCX. Ronda 2 serializó la subida con orden global `convocatoria → curso → tesis`, cerró Esquema también para excepciones, añadió límites ZIP y alineó la UI. Ronda 3 dejó de confiar en el contador EOCD y validó/contó el directorio central crudo antes de `ZipFile`, incluido EOCD falsificado. Re-review final aprobó con 0 Critical, 0 Important y 0 Minor abiertos.
- 2026-09-07: Validación independiente ejecutada sin editar código funcional. El validador reproducible en `artifacts/static_validator.py` terminó con exit code 0: 17 Python AST, 7 XML, 2 ACL, manifest/imports/dependencias, 53 métodos de prueba, estilo, helpers puros de cortes y formatos PDF/DOC/DOCX, 18 contratos agrupados, targets de herencia y alcance pasaron.
- 2026-09-07: Por instrucción explícita del usuario se ejecutaron 0 comandos Docker y no se accedió a base de datos. TransactionCase/HttpCase e integración PostgreSQL quedaron `skipped` con justificación. TestSprite MCP no está disponible y no se intentó túnel ni subida; `e2e_testsprite` quedó `skipped`. `verification.json` emitido con estado `passed`, sin afirmar resultados de runtime.
- 2026-09-07: Documentación posterior a Review y Validación completada sin modificar código, tests, seguridad, datos ni configuración de runtime. Se añadió la guía de administradores/revisores/desarrolladores, el índice de módulos, el changelog 16.0.1.0.0 y conocimiento reutilizable sobre preflight de ZIP/DOCX no confiable antes de `ZipFile`; no se duplicó el patrón de URLs eLearning ya cubierto por `irg_elearning_url_slide.md`.
- 2026-09-07: Autocheck documental: los 3 enlaces relativos de la guía resolvieron a archivos existentes; la fila del índice aparece una sola vez; el conteo documentado coincide con 53 métodos de test; `git diff --check` terminó con exit 0 y los checks `--no-index` de los 3 archivos nuevos no emitieron errores de whitespace (exit 1 esperado al existir diferencias). El estado Git conserva únicamente el addon, la misión/especificación, la nueva entrada knowledge y el índice documental, sin staging, commit, push ni PR.
- 2026-09-07: Incidencia beta reproducida en instalación: el pre-init buscaba `completion_proc`, nombre procedente del supuesto inicial, pero el campo real del repositorio es `op.student.course.completion_porc`. La instalación falló antes de crear tablas y la transacción quedó revertida.
- 2026-09-07: Se reabre Implementación/TDD por corrección funcional. Diseño aprobado: dependencia explícita del proveedor real, uso de `completion_porc`, cron paginado sin dominio sobre el campo no almacenado y reevaluación inmediata tras cambios ORM de `app.gradebook.result`. Por instrucción del usuario no se ejecutará Docker; RED/GREEN local se evidenciará con contratos estáticos y la ejecución Odoo quedará nuevamente justificada como `skipped`.
- 2026-09-07: Security Advisor de la corrección emitió `[NO]`: exigió recalcular/persistir `final_subject_note` antes del progreso, serializar por matrícula para no perder cruces concurrentes del 50 %, limitar campos de disparo y evitar la reevaluación duplicada del `create` heredado. El plan se enmendó con bloqueo ordenado, recálculo explícito, invalidación y contexto privado; pendiente nueva revisión.
- 2026-09-07: Segunda revisión del Security Advisor emitió `[YES]`. Se aclararon también las dependencias directas de `isep_student_filter` e `isep_gradebook`; queda autorizado el paso a TDD con serialización, recálculo persistido y `sudo()` acotado.
- 2026-09-07: TDD RED ejecutado con el Python empaquetado del workspace (el alias `python` no existe en este equipo). El validador pasó los controles previos y falló con exit 1 al exigir el todavía inexistente `models/app_gradebook_result.py`; evidencia en `artifacts/progress-field-red.txt`. No se usó Docker.
- 2026-09-07: GREEN implementado en 16.0.1.0.1: dependencias directas, hook `completion_porc`, cron sin dominio sobre el campo calculado y disparador `app.gradebook.result` con lock ordenado, recálculo/flush e invalidación. Se retiraron todas las referencias a `completion_proc` del addon y se añadieron dos pruebas, total 55.
- 2026-09-07: Review independiente aprobada con 0 Critical y 0 Important. Dejó dos observaciones Minor no bloqueantes: ampliar en el futuro las pruebas específicas de `write/unlink` y documentar que el progreso se correlaciona por alumno+curso, no por lote.
- 2026-09-07: Validación independiente `passed`: 13 grupos estáticos, 0 fallos, `git diff --check` limpio y 0 referencias a `completion_proc` en el addon. Odoo/PostgreSQL/TestSprite permanecieron `skipped` por la prohibición expresa de Docker.
- 2026-09-07: Documentación actualizada a 16.0.1.0.1: campo/proveedor real, naturaleza no almacenada, trigger de notas, correlación alumno+curso, 55 tests, changelog y knowledge reutilizable. Comprobación final acotada: validador 13/13, `git diff --check` exit 0, `verification.json` parseable con estado `passed` y alcance Git limitado a la corrección y sus artefactos.
- 2026-09-07: Beta reportó `ParseError` al instalar: el XPath `//div[hasclass('o_wslides_slides_list_slide')]` no existe en `website_slides.course_slides_list_slide`. El padre oficial Odoo 16 usa un `li` raíz con `t-attf-class="o_wslides_slides_list_slide ..."`; el addon local `irg_elearning_url_slide` confirma además la estructura interior mediante su anchor de enlace. Se reabre TDD para corregir exclusivamente el selector, sin Docker.
- 2026-09-07: TDD RED del XPath ejecutado. Tras corregir dos errores del propio harness (`etree` y `.xpath` no pertenecían al validador stdlib), el contrato llegó a la aserción funcional y falló únicamente como `elearning_content_xpath_matches_odoo16_parent`; evidencia en `artifacts/qweb-xpath-red.txt`.
- 2026-09-07: GREEN QWeb en 16.0.1.0.2: el `t-if` se aplica al `li` raíz real mediante `//li[contains(@t-attf-class, 'o_wslides_slides_list_slide')]`. Validador local: 13 grupos, 20 contratos y 0 fallos.
- 2026-09-07: Review independiente aprobada con 0 Critical, 0 Important y 0 Minor. Confirmó una coincidencia raíz, composición con otras herencias y permanencia del control server-side.
- 2026-09-07: Validación independiente `passed`: siete XML válidos, XPath evaluado con una coincidencia contra el padre oficial Odoo 16, manifest 16.0.1.0.2 y `git diff --check` exit 0. Runtime Odoo/TestSprite quedaron `skipped` sin usar Docker.
- 2026-09-08: La aceptación beta completó activación, dos versiones de Esquema, asignación y apertura de Entrega parcial. Detectó cuatro regresiones de presentación/integración: lista backend sin columnas, copy del portal poco operativo, creación de sección con `is_category` readonly y canal/contenido Online incorrectos.
- 2026-09-08: Diagnóstico de causa raíz: el one2many carece de tree; portal y membership usan siempre el canal configurado; el router de modalidades consulta `op.admission` en lugar de la matrícula TFM; la autorización exige igualdad exacta de canal; y el bootstrap no incluye convocatorias. Plan ampliado antes de cambios funcionales. El usuario aprobó implementar con «adelante» y mantuvo la prohibición de usar Docker.
- 2026-09-08: Security Advisor beta ronda 1 emitió `[NO]`: identificó efectos laterales de los hooks V2 sobre memberships hermanas, falta de conciliación al romper/reemplazar la familia de canales y persistencia de decisiones por admisión global en las rutas directas. El plan se enmendó con doble contexto de aislamiento, lifecycle `slide.channel`, gate exacto en canal+slide, búsqueda completa antes de filtrar y fallback de clones fail-closed. Pendiente segunda revisión.
- 2026-09-08: Security Advisor beta ronda 2 emitió `[YES]`; autorizó iniciar Implementación/TDD con los controles enmendados.
- 2026-09-08: TDD RED ejecutado sin Docker. Se añadieron ocho regresiones ORM/HTTP de aceptación (63 tests totales) y nueve contratos estáticos. El validador alcanzó las nuevas aserciones y falló con exit 1 exclusivamente porque la implementación aún no existe; evidencia en `artifacts/beta-acceptance-red.txt`.
- 2026-09-08: GREEN 16.0.1.0.3 implementado por herencia: canal efectivo desde la matrícula exacta, gate de rutas TFM posterior al controlador Online final, memberships aisladas de hooks V2, lifecycle de familia, fallback seguro de categorías clonadas, bootstrap futuro, listado backend con columnas, ocultación legacy, `force_save` y copy/fechas del portal. Validador 13/13 y 29 contratos, compileall y `git diff --check` terminaron con exit 0; evidencia en `artifacts/beta-acceptance-green.txt`. No se usó Docker.
- 2026-09-08: Review independiente beta no aprobó la primera versión: detectó un
  fail-open de clones inválidos, pérdida de la cadena Online en slides no TFM,
  relaciones inversas omitidas en lifecycle, elevación prematura y huecos de
  pruebas. Se reabrió Implementación/TDD.
- 2026-09-08: TDD RED 2 amplió la suite a 69 métodos. El validador terminó con
  exit 1 exactamente por seis contratos derivados del Review antes de tocar de
  nuevo código productivo. No se usó Docker por instrucción expresa.
- 2026-09-08: GREEN 2 cerró los seis contratos y añadió defensa para familias
  con backlinks rotos. La suite quedó en 70 métodos y 35 contratos; el
  validador local pasó 13/13, compileall y `git diff --check` terminaron con
  exit 0. Review independiente ronda 2: APROBADO, sin observaciones Critical,
  Important ni Minor. No se usó Docker.
- 2026-09-08: Validación independiente final `passed`: base `7a14590fe`, versión
  16.0.1.0.3, 13 grupos, 70 pruebas estructurales, 35 contratos, compileall,
  XML/manifiesto y `git diff --check` sin fallos. Odoo/PostgreSQL/TestSprite se
  registraron como `skipped` por la prohibición de Docker, sin afirmar runtime.
- 2026-09-08: Documentación posterior a gates actualizada: guía 16.0.1.0.3,
  configuración base/Online, diferencia entre Secciones iRG y Contenido,
  checklist beta, changelog y corrección de `completion_porc` en el micro-spec.
- 2026-09-08: Comprobación final acotada: `verification.json` parseable y
  `passed`, versión manifest/guía coincidente, conteo documentado de 70 pruebas,
  tres enlaces resueltos, cero `__pycache__` en el addon y `git diff --check`
  exit 0. No se repitió Review/Validación porque Documentación no cambió runtime.
