Runtime validation constraints — QWeb XPath correction — 2026-09-07

- Odoo module installation and TransactionCase/HttpCase: SKIPPED. The user
  explicitly prohibited opening, querying or executing Docker on this computer.
  Repository policy requires Odoo validation through docker-compose.local.yml,
  so no alternate unapproved runtime was used and no runtime result is claimed.
- PostgreSQL/Odoo integration and concurrency: SKIPPED for the same reason. The
  static validator confirms 55 Odoo test methods are structurally present, but
  those tests were not executed.
- e2e_testsprite: SKIPPED. The corrected diff touches a QWeb view, so E2E is in
  scope, but TestSprite MCP is unavailable in this session and its mandatory
  disposable local Odoo target on port 8069 cannot be started while Docker is
  prohibited. No code upload, tunnel or beta/production access was attempted.
- Database: unavailable and not accessed. No fixtures or services were created,
  so cleanup/restoration is not applicable.
- Docker commands executed: 0.
- TestSprite calls executed: 0.

Compensating evidence consists of complete XML parsing, a direct lxml evaluation
of the corrected selector with exactly one match against the official Odoo 16
parent-node fixture, the repository static contract suite and git diff checks.
