# Changelog — irg-student-degree-type

## 16.0.1.0.0 — 2026-08-28

- Nuevo módulo `irg_student_degree_type`.
- Catálogo `irg.student.degree.type` (nombre + color).
- Campo `op.student.irg_degree_type_ids` con widget de etiquetas en la
  ficha de alumno, tras el contacto de emergencia.
