diff --git a/addons-extra/extrairg/irg_certificate_partial/models/irg_certificate_request.py b/addons-extra/extrairg/irg_certificate_partial/models/irg_certificate_request.py
index 3d5d83d0..14515674 100644
--- a/addons-extra/extrairg/irg_certificate_partial/models/irg_certificate_request.py
+++ b/addons-extra/extrairg/irg_certificate_partial/models/irg_certificate_request.py
@@ -89,7 +89,7 @@ class IrgCertificateRequest(models.Model):
             if run.text:
                 run.text = run.text.lstrip()
             if is_physical:
-                run.font.size = Pt(10)
+                run.font.size = Pt(8.5)
         return paragraph
 
     def _replace_dpto_academico_intro(self, doc):
@@ -121,7 +121,7 @@ class IrgCertificateRequest(models.Model):
                     para.text = f'Para que así conste, firmo la presente en Barcelona, a fecha {fecha_larga}'
                     para.paragraph_format.space_after = Pt(48)
                     for run in para.runs:
-                        run.font.size = Pt(10)
+                        run.font.size = Pt(8.5)
                 self._format_partial_body_paragraph(para, justify=True)
 
     def _format_partial_static_paragraphs(self, doc):
@@ -290,7 +290,7 @@ class IrgCertificateRequest(models.Model):
                                         if embed_nodes:
                                             para._p.remove(run._r)
                                             break
-        scale_percent = 100 if is_physical else 75
+        scale_percent = 85 if is_physical else 75
         self._scale_document_fonts(doc, percent=scale_percent)
 
         top_font_size = None
diff --git a/addons-extra/extrairg/irg_certificate_partial/tests/test_partial.py b/addons-extra/extrairg/irg_certificate_partial/tests/test_partial.py
index 54325e60..ce034a09 100644
--- a/addons-extra/extrairg/irg_certificate_partial/tests/test_partial.py
+++ b/addons-extra/extrairg/irg_certificate_partial/tests/test_partial.py
@@ -703,11 +703,11 @@ class TestIrgCertificatePartial(TransactionCase):
         # 1. Verify top margin shift (72 Pt default + 37.5 Pt shift = 109.5 Pt)
         self.assertEqual(doc.sections[0].top_margin.pt, 109.5)
         
-        # 2. Verify outer font size is 10 Pt (not scaled down by 75%)
+        # 2. Verify outer font size is 8.5 Pt
         body_runs = [r for p in doc.paragraphs if p.text.strip() for r in p.runs if r.font and r.font.size]
         self.assertTrue(len(body_runs) > 0, "No body runs found with font size set.")
         for r in body_runs:
-            self.assertEqual(r.font.size.pt, 10.0, f"Outer text run '{r.text}' font size is {r.font.size.pt}, expected 10.0 Pt")
+            self.assertEqual(r.font.size.pt, 8.5, f"Outer text run '{r.text}' font size is {r.font.size.pt}, expected 8.5 Pt")
             
         # 3. Verify table run font size is 7.5 Pt (95250 EMUs)
         self._assert_table_font_sizes_match_value(res_file, Pt(7.5))
diff --git a/addons-extra/extrairg/irg_gradebook_certificates/models/irg_certificate_request.py b/addons-extra/extrairg/irg_gradebook_certificates/models/irg_certificate_request.py
index 748051c8..44d3aad4 100644
--- a/addons-extra/extrairg/irg_gradebook_certificates/models/irg_certificate_request.py
+++ b/addons-extra/extrairg/irg_gradebook_certificates/models/irg_certificate_request.py
@@ -541,7 +541,7 @@ class IrgCertificateRequest(models.Model):
             if run.text:
                 run.text = run.text.lstrip()
             if is_physical:
-                run.font.size = Pt(10)
+                run.font.size = Pt(8.5)
         return paragraph
 
     def _format_gradebook_static_paragraphs(self, doc):
@@ -585,7 +585,7 @@ class IrgCertificateRequest(models.Model):
                     para.text = f'Para que así conste, firmo la presente en Barcelona, a fecha {fecha_larga}'
                     para.paragraph_format.space_after = Pt(48)
                     for run in para.runs:
-                        run.font.size = Pt(10)
+                        run.font.size = Pt(8.5)
                 self._format_gradebook_body_paragraph(para, justify=True)
             if normalized_text in signature_texts:
                 self._format_gradebook_signature_paragraph(para)
@@ -999,7 +999,7 @@ class IrgCertificateRequest(models.Model):
                                         if embed_nodes:
                                             para._p.remove(run._r)
                                             break
-        scale_percent = 100 if is_physical else 75
+        scale_percent = 85 if is_physical else 75
         self._scale_document_fonts(doc, percent=scale_percent)
         self._replace_dpto_academico_intro(doc)
 
diff --git a/addons-extra/extrairg/irg_gradebook_certificates/tests/test_certificate_request.py b/addons-extra/extrairg/irg_gradebook_certificates/tests/test_certificate_request.py
index 5ca7574d..d5823968 100644
--- a/addons-extra/extrairg/irg_gradebook_certificates/tests/test_certificate_request.py
+++ b/addons-extra/extrairg/irg_gradebook_certificates/tests/test_certificate_request.py
@@ -725,11 +725,11 @@ class TestIrgCertificateRequest(TransactionCase):
         # 1. Verify top margin shift (72 Pt default + 37.5 Pt shift = 109.5 Pt)
         self.assertEqual(doc.sections[0].top_margin.pt, 109.5)
         
-        # 2. Verify outer font size is 10 Pt (not scaled down by 75%)
+        # 2. Verify outer font size is 8.5 Pt
         body_runs = [r for p in doc.paragraphs if p.text.strip() for r in p.runs if r.font and r.font.size]
         self.assertTrue(len(body_runs) > 0, "No body runs found with font size set.")
         for r in body_runs:
-            self.assertEqual(r.font.size.pt, 10.0, f"Outer text run '{r.text}' font size is {r.font.size.pt}, expected 10.0 Pt")
+            self.assertEqual(r.font.size.pt, 8.5, f"Outer text run '{r.text}' font size is {r.font.size.pt}, expected 8.5 Pt")
             
         # 3. Verify table run font size is 7.5 Pt (95250 EMUs)
         self._assert_table_font_sizes_match_value(res_file, Pt(7.5))
