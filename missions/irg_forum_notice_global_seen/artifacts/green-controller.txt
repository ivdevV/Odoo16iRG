Task 3 GREEN — secure controller/global discovery routes

Command:
docker compose -f /Users/ivrogo/Workspace/Proyectos\ iRG/Odoo16iRG/docker-compose.local.yml -f missions/irg_forum_notice_global_seen/docker-compose.worktree.yml run --rm --no-deps odoo_local odoo -c /etc/odoo/odoo.conf -d test_irg_forum_global_seen -i irg_forum_notice_global_seen --test-enable --test-tags /irg_forum_notice_global_seen --stop-after-init --log-level=test

Selected coverage:
- Both inherited discovery routes suppress the same globally seen post.
- Both inherited mark-seen routes write global state; the campus-wide shape
  accepts no `course_id`.
- Public, malformed, missing, internal-invisible, and portal-invisible mark
  requests are rejected without creating global rows.
- Internal and portal direct ORM and generic `call_kw` attempts cannot forge
  legacy/global state; counts and row values remain unchanged after denials.
- `_irg_is_seen` and `_irg_mark_seen` are rejected by generic RPC as private.
- Trusted sudo/controller writes and direct system legacy administration pass.

odoo.tests.stats: irg_forum_notice_global_seen: 24 tests 3.45s 1899 queries
odoo.tests.result: 0 failed, 0 error(s) of 20 tests when loading database
'test_irg_forum_global_seen'
Exit code: 0.

Static checks run in the same command chain:
- `python3 -m py_compile` for controller and test module: PASS.
- `git diff --check`: PASS.

The runtime emitted only pre-existing warnings from unrelated addons (missing
manifest licenses and existing `forum.forum` field parameters).

Reviewer regression GREEN — no course context:

Command:
docker compose -f /Users/ivrogo/Workspace/Proyectos\ iRG/Odoo16iRG/docker-compose.local.yml -f missions/irg_forum_notice_global_seen/docker-compose.worktree.yml run --rm --no-deps odoo_local odoo -c /etc/odoo/odoo.conf -d test_irg_forum_global_seen -i irg_forum_notice_global_seen --test-enable --test-tags /irg_forum_notice_global_seen --stop-after-init --log-level=test

Selected output:

```text
Starting TestForumNoticeGlobalSeenHttp.test_any_campus_suppresses_seen_post_without_course_context ...
odoo.tests.stats: irg_forum_notice_global_seen: 25 tests 3.94s 2047 queries
odoo.tests.result: 0 failed, 0 error(s) of 21 tests when loading database
'test_irg_forum_global_seen'
```

Exit code: 0. The same command chain then ran `python3 -m py_compile` for
controller/tests and `git diff --check`; both exited successfully.
