<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- View res.partner form -->
    <record id="view_res_partner_phone_form" model="ir.ui.view">
        <field name="name">view.res.partner.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='phone']" position="replace">
                <field name="is_manager" invisible="1"/>
                <field name="phone" string="Teléfono" attrs="{'invisible': [('is_manager', '!=', True)]}"/>
                <field name="phone" string="Teléfono" password="True" attrs="{'invisible': [('is_manager', '=', True)]}"/>
            </xpath>

            <xpath expr="//field[@name='mobile']" position="replace">
                <field name="is_manager" invisible="1"/>
                <field name="mobile" string="Móvil" attrs="{'invisible': [('is_manager', '!=', True)]}"/>
                <field name="mobile" string="Móvil" password="True" attrs="{'invisible': [('is_manager', '=', True)]}"/>
            </xpath>
        </field>
    </record>

    <!-- View res.partner tree -->
    <record id="view_res_partner_phone_tree" model="ir.ui.view">
        <field name="name">view.res.partner.tree</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='phone']" position="replace">
                <field name="is_manager" invisible="1"/>
                <field name="phone" string="Teléfono" attrs="{'invisible': [('is_manager', '!=', True)]}" optional="show"/>
            </xpath>
        </field>
    </record>

</odoo>