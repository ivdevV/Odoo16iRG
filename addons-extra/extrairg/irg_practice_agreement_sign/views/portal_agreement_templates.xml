<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_agreement_page" name="Firma de Convenio Marco de Prácticas">
        <t t-call="portal.portal_layout">
            <div class="container py-4">
                <div class="row justify-content-center">
                    <div class="col-lg-10">
                        
                        <!-- Tarjeta Principal -->
                        <div class="card shadow-sm border-0 mb-4">
                            <div class="card-header bg-primary text-white p-4">
                                <h3 class="mb-1 text-white">Convenio Marco de Colaboración Educativa</h3>
                                <p class="mb-0 text-white-50">Instituto Raimon Gaja (iRG) &amp; <t t-out="agreement.center_official_name or agreement.practice_center_id.name"/></p>
                            </div>

                            <div class="card-body p-4">
                                
                                <t t-if="error">
                                    <div class="alert alert-danger mb-4" role="alert">
                                        <t t-out="error"/>
                                    </div>
                                </t>

                                <t t-if="agreement.state == 'completed'">
                                    <!-- ESTADO COMPLETADO / YA FIRMADO -->
                                    <div class="alert alert-success p-4 mb-4 text-center">
                                        <h4 class="alert-heading font-weight-bold mb-2">¡Convenio Firmado Correctamente!</h4>
                                        <p class="mb-2">Este convenio fue firmado digitalmente el <strong><t t-out="agreement.signed_on"/></strong> por <strong><t t-out="agreement.signed_by"/></strong>.</p>
                                        <div class="mt-3">
                                            <a t-att-href="'/convenio/descargar/' + token" class="btn btn-success btn-lg">
                                                <i class="fa fa-download mr-2"></i> Descargar Convenio PDF Firmado
                                            </a>
                                        </div>
                                    </div>
                                </t>

                                <t t-else="">
                                    <!-- FORMULARIO DE REVISIÓN Y FIRMA -->
                                    <form t-att-action="'/convenio/firma/' + token + '/submit'" method="post" id="agreement_sign_form">
                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        <input type="hidden" name="signature_data" id="signature_data_input"/>

                                        <h5 class="text-primary border-bottom pb-2 mb-3">1. Datos de la Entidad Colaboradora</h5>
                                        <div class="row">
                                            <div class="col-md-6 form-group">
                                                <label class="font-weight-bold">Nombre / Razón Social del Centro *</label>
                                                <input type="text" name="center_official_name" class="form-control" t-att-value="agreement.center_official_name or agreement.practice_center_id.name" required="1"/>
                                            </div>
                                            <div class="col-md-6 form-group">
                                                <label class="font-weight-bold">CIF / NIF del Centro *</label>
                                                <input type="text" name="center_vat" class="form-control" t-att-value="agreement.center_vat" required="1"/>
                                            </div>
                                            <div class="col-md-6 form-group">
                                                <label class="font-weight-bold">Nombre del Representante Legal *</label>
                                                <input type="text" name="signatory_name" class="form-control" t-att-value="agreement.signatory_name" required="1"/>
                                            </div>
                                            <div class="col-md-6 form-group">
                                                <label class="font-weight-bold">Cargo del Representante *</label>
                                                <input type="text" name="signatory_title" class="form-control" t-att-value="agreement.signatory_title or 'Representante Legal'" required="1"/>
                                            </div>
                                        </div>

                                        <div class="row">
                                            <div class="col-md-6 form-group">
                                                <label class="font-weight-bold">Dirección de la Sede</label>
                                                <input type="text" name="street" class="form-control" t-att-value="agreement.street"/>
                                            </div>
                                            <div class="col-md-3 form-group">
                                                <label class="font-weight-bold">Ciudad</label>
                                                <input type="text" name="city" class="form-control" t-att-value="agreement.city"/>
                                            </div>
                                            <div class="col-md-3 form-group">
                                                <label class="font-weight-bold">Código Postal</label>
                                                <input type="text" name="zip" class="form-control" t-att-value="agreement.zip"/>
                                            </div>
                                        </div>

                                        <!-- Vista Previa del Convenio -->
                                        <h5 class="text-primary border-bottom pb-2 my-4">2. Resumen de Cláusulas del Convenio</h5>
                                        <div class="bg-light p-3 rounded border mb-4" style="max-height: 250px; overflow-y: auto; font-size: 13px;">
                                            <p><strong>REUNIDOS:</strong> De una parte iRG (Instituto Raimon Gaja, S.L., NIF B 564 88687) y de otra parte la Entidad Colaboradora especificada arriba.</p>
                                            <p><strong>1. OBJETO:</strong> Establecer los términos para que los estudiantes de iRG puedan realizar prácticas profesionales externas.</p>
                                            <p><strong>2. FORMALIZACIÓN:</strong> Se formaliza mediante anexos/convenios específicos para cada estudiante.</p>
                                            <p><strong>3. NATURALEZA:</strong> Formativa, sin relación laboral entre el estudiante y la entidad.</p>
                                            <p><strong>4. MARCAS Y VIGENCIA:</strong> Vigencia de 1 año renovable tácitamente. Autorización genérica de uso de logotipo durante el convenio.</p>
                                            <p><strong>5. CONFIDENCIALIDAD Y RGPD:</strong> Cumplimiento estricto del secreto profesional y de la normativa de protección de datos (UE 2016/679).</p>
                                        </div>

                                        <!-- Canvas de Firma Digital -->
                                        <h5 class="text-primary border-bottom pb-2 mb-3">3. Firma Digital del Representante Legal</h5>
                                        <p class="text-muted small">Por favor, dibuje su firma en el recuadro inferior utilizando el ratón o la pantalla táctil de su dispositivo.</p>

                                        <div class="card mb-3">
                                            <div class="card-body text-center p-2 bg-white">
                                                <canvas id="signature_canvas" width="600" height="200" style="border: 2px dashed #0056b3; border-radius: 6px; cursor: crosshair; touch-action: none; background-color: #ffffff; max-width: 100%;"></canvas>
                                            </div>
                                            <div class="card-footer bg-light d-flex justify-content-between align-items-center">
                                                <button type="button" class="btn btn-outline-secondary btn-sm" id="clear_signature_btn">
                                                    <i class="fa fa-eraser mr-1"></i> Limpiar Firma
                                                </button>
                                                <span class="text-muted small">Firma en el espacio punteado</span>
                                            </div>
                                        </div>

                                        <div class="text-center mt-4">
                                            <button type="submit" class="btn btn-primary btn-lg px-5 font-weight-bold" id="submit_sign_btn">
                                                <i class="fa fa-check-circle mr-2"></i> Firmar y Enviar Convenio
                                            </button>
                                        </div>
                                    </form>
                                </t>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

            <!-- JavaScript para el Canvas de Firma Táctil / Ratón -->
            <script type="text/javascript">
                document.addEventListener("DOMContentLoaded", function () {
                    var canvas = document.getElementById("signature_canvas");
                    if (!canvas) return;

                    var ctx = canvas.getContext("2d");
                    var drawing = false;
                    var hasSigned = false;

                    ctx.lineWidth = 2.5;
                    ctx.lineCap = "round";
                    ctx.strokeStyle = "#000080";

                    function getPos(e) {
                        var rect = canvas.getBoundingClientRect();
                        var clientX = e.touches ? e.touches[0].clientX : e.clientX;
                        var clientY = e.touches ? e.touches[0].clientY : e.clientY;
                        return {
                            x: clientX - rect.left,
                            y: clientY - rect.top
                        };
                    }

                    function startDrawing(e) {
                        drawing = true;
                        hasSigned = true;
                        var pos = getPos(e);
                        ctx.beginPath();
                        ctx.moveTo(pos.x, pos.y);
                    }

                    function draw(e) {
                        if (!drawing) return;
                        e.preventDefault();
                        var pos = getPos(e);
                        ctx.lineTo(pos.x, pos.y);
                        ctx.stroke();
                    }

                    function stopDrawing() {
                        drawing = false;
                    }

                    canvas.addEventListener("mousedown", startDrawing);
                    canvas.addEventListener("mousemove", draw);
                    canvas.addEventListener("mouseup", stopDrawing);
                    canvas.addEventListener("mouseleave", stopDrawing);

                    canvas.addEventListener("touchstart", startDrawing, {passive: false});
                    canvas.addEventListener("touchmove", draw, {passive: false});
                    canvas.addEventListener("touchend", stopDrawing);

                    document.getElementById("clear_signature_btn").addEventListener("click", function () {
                        ctx.clearRect(0, 0, canvas.width, canvas.height);
                        hasSigned = false;
                    });

                    document.getElementById("agreement_sign_form").addEventListener("submit", function (e) {
                        if (!hasSigned) {
                            alert("Por favor, firme en el recuadro antes de enviar el convenio.");
                            e.preventDefault();
                            return false;
                        }
                        var dataURL = canvas.toDataURL("image/png");
                        document.getElementById("signature_data_input").value = dataURL;
                    });
                });
            </script>
        </t>
    </template>
</odoo>
