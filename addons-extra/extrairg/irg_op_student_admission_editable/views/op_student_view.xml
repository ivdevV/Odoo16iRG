<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!--
            Override the inline form that opens when clicking an admission row
            in the "Educativo" tab of the op.student form view.

            Priority 24 ensures this runs after isep_student_filter (priority 16)
            has already added the op_admission_ids field to the student view.
        -->
        <record id="view_op_student_admission_editable" model="ir.ui.view">
            <field name="name">view.op.student.admission.editable</field>
            <field name="model">op.student</field>
            <field name="inherit_id" ref="openeducat_core.view_op_student_form"/>
            <field name="priority" eval="24"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='op_admission_ids']/form" position="replace">
                    <form string="Detalle de admisión">
                        <group>
                            <group>
                                <field name="admission_date"/>
                                <field name="application_number" string="Matrícula" readonly="1"/>
                                <field name="course_id" options="{'no_create': True}"/>
                            </group>
                            <group>
                                <field name="batch_id" options="{'no_create': True}"
                                       context="{'course_id': course_id, 'get_parent_batch': 1}"/>
                                <field name="due_date"/>
                                <field name="state" readonly="1"/>
                            </group>
                        </group>

                        <separator string="Asignaturas"/>
                        <!--
                            Provide both tree AND inline form so Odoo uses our form when
                            clicking a subject row, bypassing app_gradebook_subject_form.
                            This inline form has NO readonly restriction on gradebook_result_ids,
                            so scoring_total is always editable regardless of subject state.
                        -->
                        <field name="gradebook_subject_ids" nolabel="1">
                            <tree string="Asignaturas" create="0" edit="1" delete="0">
                                <field name="name" string="Asignatura"/>
                                <field name="point_average_assignment" string="AVG Asignaciones"/>
                                <field name="point_average_exam" string="AVG Exámenes"/>
                                <field name="point_average_interaction" string="AVG Interacciones"/>
                                <field name="point_average_foro" string="AVG Foro"/>
                                <field name="final_subject_note" string="Calificación final"/>
                            </tree>
                            <form string="Asignatura" create="0" edit="1" delete="0">
                                <field name="show_exam" invisible="1"/>
                                <field name="admission_id" invisible="1"/>
                                <field name="course_id" invisible="1"/>
                                <field name="gradebook_student_id" invisible="1"/>
                                <sheet>
                                    <div class="oe_title">
                                        <h2><field name="name"/></h2>
                                    </div>
                                    <group>
                                        <group>
                                            <field name="op_subject_id" required="1"
                                                   options="{'no_open': True, 'no_create': True}"/>
                                            <field name="final_subject_note"/>
                                        </group>
                                        <group>
                                            <label for="point_average_exam"
                                                   attrs="{'invisible': [('show_exam','=', False)]}"/>
                                            <div class="d-flex"
                                                 attrs="{'invisible': [('show_exam','=', False)]}">
                                                <field name="point_average_exam" class="oe_inline mx-3"/>
                                                <span class="oe_inline o_form_label mx-3">=> </span>
                                                <field name="info_exam"/>
                                            </div>
                                        </group>
                                    </group>
                                    <notebook>
                                        <page string="Evaluaciones" name="exams">
                                            <!-- No attrs readonly — scoring_total is always editable -->
                                            <field name="gradebook_result_ids"
                                                   context="{'default_admission_id': admission_id}">
                                                <tree editable="bottom" delete="0">
                                                    <field name="survey_type" optional="show" required="1"/>
                                                    <field name="description"/>
                                                    <field name="name" optional="hide" readonly="0" force_save="1"/>
                                                    <field name="scoring_total"/>
                                                    <field name="post_qty"
                                                           attrs="{'invisible': [('survey_type','!=','foro')]}"/>
                                                    <field name="comment" optional="hide"/>
                                                    <field name="gradebook_subject_id" optional="hide"/>
                                                </tree>
                                            </field>
                                        </page>
                                        <page string="Opciones" name="options">
                                            <group>
                                                <group>
                                                    <field name="gradebook_id" options="{'no_create': True}"/>
                                                </group>
                                            </group>
                                        </page>
                                    </notebook>
                                </sheet>
                            </form>
                        </field>
                    </form>
                </xpath>

            </field>
        </record>

        <!--
            Override app.gradebook.subject form to allow editing scoring_total
            even when the subject state is 'done' (FINALIZADO).
            The original view makes gradebook_result_ids fully readonly in done state,
            which prevents editing any evaluation score.
        -->
        <record id="view_gradebook_subject_scoring_editable" model="ir.ui.view">
            <field name="name">view.gradebook.subject.scoring.editable</field>
            <field name="model">app.gradebook.subject</field>
            <field name="inherit_id" ref="isep_gradebook.app_gradebook_subject_form"/>
            <field name="arch" type="xml">
                <!-- Remove the done-state readonly lock from the evaluations tree -->
                <xpath expr="//field[@name='gradebook_result_ids']" position="attributes">
                    <attribute name="attrs">{}</attribute>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
