"""irg_language_nav module init

This module has no Python models; keep init minimal to avoid import errors.
"""

# Minimal package initializer: do NOT import a non-existent `models` package here.
# Importing `models` when that package is missing caused a circular ImportError
# during module installation in the Odoo server. Any model code should live in
# a `models` package and be imported lazily by Odoo when present.

__all__ = []
