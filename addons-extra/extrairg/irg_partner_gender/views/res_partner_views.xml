<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_gender" model="ir.ui.view">
        <field name="name">res.partner.form.irg.gender</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="before">
                <field name="gender" widget="radio" options="{'horizontal': true}"/>
            </xpath>
            <!-- Moodle tab: username must not block contact edits -->
            <xpath expr="//page[@name='moodle']//field[@name='username']" position="attributes">
                <attribute name="required">0</attribute>
            </xpath>
        </field>
    </record>
</odoo>
