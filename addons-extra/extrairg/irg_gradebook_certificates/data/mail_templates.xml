<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ============================================================
             1. Digital certificate ready — sent to student
        ============================================================ -->
        <record id="mail_template_cert_digital_ready" model="mail.template">
            <field name="name">Certificado Digital — Listo para descargar</field>
            <field name="model_id" ref="model_irg_certificate_request"/>
            <field name="subject">Tu certificado de notas digital ya está disponible ({{ object.name }})</field>
            <field name="email_to">{{ object.partner_id.email }}</field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="body_html"><![CDATA[
<div style="font-family: Arial, sans-serif; font-size: 14px; color: #333;">
    <p>Estimado/a <strong>{{ object.partner_id.name }}</strong>,</p>
    <p>
        Tu <strong>certificado de notas digital</strong> con referencia
        <strong>{{ object.name }}</strong> ya está disponible para su descarga.
    </p>
    <p>
        Puedes descargarlo accediendo al portal del alumno en la sección
        <em>Mis Certificados</em> o a través del siguiente enlace:
    </p>
    <p style="text-align:center; margin: 20px 0;">
        <a href="{{ ctx.get('base_url', '') }}/campus/certificates/download/{{ object.id }}"
           style="background-color:#875A7B;color:#fff;padding:10px 20px;border-radius:4px;text-decoration:none;">
            Descargar Certificado
        </a>
    </p>
    <p>Si tienes alguna duda, no dudes en contactarnos.</p>
    <p>Un cordial saludo,<br/><strong>{{ object.env.company.name }}</strong></p>
</div>
]]></field>
            <field name="auto_delete" eval="True"/>
        </record>

        <!-- ============================================================
             2. Physical certificate payment confirmed — admin notified
        ============================================================ -->
        <record id="mail_template_cert_paid_physical" model="mail.template">
            <field name="name">Certificado Físico — Pago recibido</field>
            <field name="model_id" ref="model_irg_certificate_request"/>
            <field name="subject">Certificado físico en preparación — {{ object.name }}</field>
            <field name="email_to">{{ object.partner_id.email }}</field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="body_html"><![CDATA[
<div style="font-family: Arial, sans-serif; font-size: 14px; color: #333;">
    <p>Estimado/a <strong>{{ object.partner_id.name }}</strong>,</p>
    <p>
        Hemos recibido el pago de tu solicitud de
        <strong>certificado de notas físico</strong>
        (referencia: <strong>{{ object.name }}</strong>).
    </p>
    <p>
        Nuestro equipo procederá a la preparación e impresión del documento.
        Te notificaremos en cuanto sea enviado&#8203;
        % if object.shipping_type == 'national':
        por mensajería nacional.
        % else:
        por mensajería internacional.
        % endif
    </p>
    <p>
        <strong>Tipo:</strong> {{ dict(object.fields_get(['certificate_type'])['certificate_type']['selection']).get(object.certificate_type, '') }}<br/>
        <strong>Envío:</strong> {{ dict(object.fields_get(['shipping_type'])['shipping_type']['selection']).get(object.shipping_type, '') }}<br/>
        <strong>Total abonado:</strong> {{ '%.2f' % object.price_total }} €
    </p>
    <p>Un cordial saludo,<br/><strong>{{ object.env.company.name }}</strong></p>
</div>
]]></field>
            <field name="auto_delete" eval="True"/>
        </record>

        <!-- ============================================================
             3. Physical certificate shipped
        ============================================================ -->
        <record id="mail_template_cert_sent" model="mail.template">
            <field name="name">Certificado — Enviado</field>
            <field name="model_id" ref="model_irg_certificate_request"/>
            <field name="subject">Tu certificado ha sido enviado — {{ object.name }}</field>
            <field name="email_to">{{ object.partner_id.email }}</field>
            <field name="lang">{{ object.partner_id.lang }}</field>
            <field name="body_html"><![CDATA[
<div style="font-family: Arial, sans-serif; font-size: 14px; color: #333;">
    <p>Estimado/a <strong>{{ object.partner_id.name }}</strong>,</p>
    <p>
        Tu certificado de notas (referencia: <strong>{{ object.name }}</strong>)
        ha sido enviado el
        <strong>{{ object.delivery_date.strftime('%d/%m/%Y') if object.delivery_date else '' }}</strong>.
    </p>
    % if object.tracking_number:
    <p>
        Número de seguimiento: <strong>{{ object.tracking_number }}</strong>
    </p>
    % endif
    <p>Un cordial saludo,<br/><strong>{{ object.env.company.name }}</strong></p>
</div>
]]></field>
            <field name="auto_delete" eval="True"/>
        </record>

        <!-- ============================================================
             4. Physical certificate payment — notify academic team
        ============================================================ -->
        <record id="mail_template_cert_physical_team" model="mail.template">
            <field name="name">Certificado Físico — Aviso Equipo Académico</field>
            <field name="model_id" ref="model_irg_certificate_request"/>
            <field name="subject">Nueva solicitud de certificado físico pagado: {{ object.name }}</field>
            <field name="email_to">academica@institutoraimongaja.com</field>
            <field name="body_html"><![CDATA[
<div style="font-family: Arial, sans-serif; font-size: 14px; color: #333;">
    <p><strong>Nueva solicitud de certificado físico — Pago recibido</strong></p>
    <p>Se ha recibido el pago de una solicitud de certificado físico que requiere preparación manual:</p>
    <table style="border-collapse: collapse; width: 100%; margin: 12px 0;">
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold; width: 40%;">Referencia</td>
            <td style="padding: 5px 10px;">{{ object.name }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Alumno</td>
            <td style="padding: 5px 10px;">{{ object.partner_id.name }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Email alumno</td>
            <td style="padding: 5px 10px;">{{ object.partner_id.email or '' }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Programa</td>
            <td style="padding: 5px 10px;">{{ object.course_id.name or '' }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Grupo / Promoción</td>
            <td style="padding: 5px 10px;">{{ object.batch_id.name or '' }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Tipo de documento</td>
            <td style="padding: 5px 10px;">{{ dict(object.fields_get(['document_type'])['document_type']['selection']).get(object.document_type, '') }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Tipo de certificado</td>
            <td style="padding: 5px 10px;">{{ dict(object.fields_get(['certificate_type'])['certificate_type']['selection']).get(object.certificate_type, '') }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Tipo de envío</td>
            <td style="padding: 5px 10px;">{{ dict(object.fields_get(['shipping_type'])['shipping_type']['selection']).get(object.shipping_type, '') }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Persona que firma</td>
            <td style="padding: 5px 10px;">{{ dict(object.fields_get(['signer'])['signer']['selection']).get(object.signer, '') }}</td>
        </tr>
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Dirección de envío</td>
            <td style="padding: 5px 10px;">
                {{ object.partner_id.street or '' }}
                % if object.partner_id.street2:
                <br/>{{ object.partner_id.street2 }}
                % endif
                <br/>{{ object.partner_id.zip or '' }} {{ object.partner_id.city or '' }}
                % if object.partner_id.state_id:
                <br/>{{ object.partner_id.state_id.name }}
                % endif
                % if object.partner_id.country_id:
                <br/>{{ object.partner_id.country_id.name }}
                % endif
            </td>
        </tr>
        % if object.custom_description:
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Peticiones adicionales</td>
            <td style="padding: 5px 10px;">{{ object.custom_description }}</td>
        </tr>
        % endif
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Importe cobrado</td>
            <td style="padding: 5px 10px;"><strong>{{ '%.2f' % object.price_total }} €</strong></td>
        </tr>
        % if object.stripe_checkout_session_id:
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Stripe Checkout Session</td>
            <td style="padding: 5px 10px;">{{ object.stripe_checkout_session_id }}</td>
        </tr>
        % endif
        % if object.stripe_payment_intent_id:
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Stripe Payment Intent</td>
            <td style="padding: 5px 10px;">{{ object.stripe_payment_intent_id }}</td>
        </tr>
        % endif
        % if object.stripe_invoice_id:
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Factura Stripe</td>
            <td style="padding: 5px 10px;">
                {{ object.stripe_invoice_id }}
                % if object.stripe_invoice_pdf:
                (<a href="{{ object.stripe_invoice_pdf }}" target="_blank">Descargar PDF</a>)
                % endif
            </td>
        </tr>
        % endif
        % if object.stripe_receipt_url:
        <tr>
            <td style="padding: 5px 10px; background: #f5f5f5; font-weight: bold;">Recibo Stripe</td>
            <td style="padding: 5px 10px;"><a href="{{ object.stripe_receipt_url }}" target="_blank">Ver Recibo</a></td>
        </tr>
        % endif
    </table>
    <p style="margin-top: 16px;">
        <a href="{{ object.get_base_url() }}/web#id={{ object.id }}&model=irg.certificate.request&view_type=form"
           style="background-color:#875A7B;color:#fff;padding:8px 16px;border-radius:4px;text-decoration:none;font-weight:bold;">
            Ver Solicitud en Odoo
        </a>
    </p>
    <p>—<br/>Sistema de Gestión Académica — {{ object.env.company.name }}</p>
</div>
]]></field>
            <field name="auto_delete" eval="True"/>
        </record>

    </data>
</odoo>
