<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_agreement_page_specific"
              inherit_id="irg_practice_agreement_sign.portal_agreement_page">
        <xpath expr="//div[hasclass('card-header')]/h3" position="replace">
            <h3 class="mb-1 text-white">
                <t t-if="agreement.agreement_type == 'especifico_internacional'">Convenio Específico de Prácticas</t>
                <t t-else="">Convenio Marco de Colaboración Educativa</t>
            </h3>
        </xpath>
        <xpath expr="//h5[contains(., 'Texto Completo')]" position="attributes">
            <attribute name="t-if">agreement.agreement_type != 'especifico_internacional'</attribute>
        </xpath>
        <xpath expr="//h5[contains(., 'Texto Completo')]/following-sibling::div[1]" position="attributes">
            <attribute name="t-if">agreement.agreement_type != 'especifico_internacional'</attribute>
        </xpath>
        <xpath expr="//h5[contains(., 'Texto Completo')]" position="after">
            <h5 t-if="agreement.agreement_type == 'especifico_internacional'"
                class="text-primary border-bottom pb-2 my-4">
                2. Texto Completo del Convenio Específico
            </h5>
            <div t-if="agreement.agreement_type == 'especifico_internacional'"
                 class="bg-white p-4 rounded border mb-4 shadow-sm"
                 style="max-height: 480px; overflow-y: auto; font-size: 13.5px; line-height: 1.6; color: #212529; border: 1px solid #ced4da;">
                <t t-set="doc" t-value="agreement"/>
                <t t-call="irg_practice_agreement_specific.agreement_document_especifico_internacional"/>
            </div>
        </xpath>
    </template>

    <template id="portal_agreement_waiting_student" name="Convenio específico: esperando firma del estudiante">
        <t t-call="portal.portal_layout">
            <div class="container py-4">
                <div class="row justify-content-center">
                    <div class="col-lg-8">
                        <div class="card shadow-sm border-0">
                            <div class="card-header bg-primary text-white p-4">
                                <h3 class="mb-1 text-white">Convenio Específico de Prácticas</h3>
                            </div>
                            <div class="card-body p-4 text-center">
                                <div class="alert alert-info">
                                    <h4 class="alert-heading">Firma del centro registrada</h4>
                                    <p class="mb-0">
                                        El convenio quedará completado cuando el estudiante firme su enlace.
                                        No es necesario volver a firmar desde este acceso.
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="portal_agreement_student_page" name="Firma de Convenio Específico (estudiante)">
        <t t-call="portal.portal_layout">
            <div class="container py-4">
                <div class="row justify-content-center">
                    <div class="col-lg-10">
                        <div class="card shadow-sm border-0 mb-4">
                            <div class="card-header bg-primary text-white p-4">
                                <h3 class="mb-1 text-white">Convenio Específico de Prácticas</h3>
                                <p class="mb-0 text-white-50">
                                    Instituto Raimon Gaja (iRG) &amp;
                                    <t t-out="agreement.center_official_name or agreement.practice_center_id.name"/>
                                </p>
                            </div>
                            <div class="card-body p-4">
                                <t t-if="error">
                                    <div class="alert alert-danger mb-4" role="alert">
                                        <t t-out="error"/>
                                    </div>
                                </t>

                                <t t-if="agreement.state == 'completed'">
                                    <div class="alert alert-success p-4 mb-4 text-center">
                                        <h4 class="alert-heading font-weight-bold mb-2">¡Convenio Firmado Correctamente!</h4>
                                        <p class="mb-2">
                                            Este convenio específico ha sido firmado por el estudiante y el centro colaborador.
                                        </p>
                                        <div class="mt-3">
                                            <a t-att-href="'/convenio/descargar/' + token" class="btn btn-success btn-lg">
                                                <i class="fa fa-download mr-2"></i> Descargar Convenio PDF Firmado
                                            </a>
                                        </div>
                                    </div>
                                </t>
                                <t t-elif="agreement.signature_student">
                                    <div class="alert alert-info p-4 mb-4 text-center">
                                        <h4 class="alert-heading">Tu firma ha sido registrada</h4>
                                        <p class="mb-0">
                                            El convenio quedará completado cuando el centro colaborador firme su enlace.
                                        </p>
                                    </div>
                                </t>
                                <t t-else="">
                                    <form t-att-action="'/convenio/firma-alumno/' + token + '/submit'" method="post" id="agreement_sign_form">
                                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                        <input type="hidden" name="signature_data" id="signature_data_input"/>

                                        <div class="form-group">
                                            <label class="font-weight-bold">Nombre del estudiante *</label>
                                            <input type="text" name="student_signer_name" class="form-control"
                                                   t-att-value="agreement.student_name" required="1"/>
                                        </div>

                                        <h5 class="text-primary border-bottom pb-2 my-4">Texto del Convenio Específico</h5>
                                        <div class="bg-white p-4 rounded border mb-4 shadow-sm"
                                             style="max-height: 480px; overflow-y: auto; font-size: 13.5px; line-height: 1.6; color: #212529; border: 1px solid #ced4da;">
                                            <t t-set="doc" t-value="agreement"/>
                                            <t t-call="irg_practice_agreement_specific.agreement_document_especifico_internacional"/>
                                        </div>

                                        <h5 class="text-primary border-bottom pb-2 mb-3">Firma Digital del Estudiante</h5>
                                        <p class="text-muted small">Dibuja tu firma en el recuadro inferior.</p>
                                        <div class="card mb-3">
                                            <div class="card-body text-center p-2 bg-white">
                                                <canvas id="signature_canvas" width="600" height="200"
                                                        style="border: 2px dashed #0056b3; border-radius: 6px; cursor: crosshair; touch-action: none; background-color: #ffffff; max-width: 100%;"></canvas>
                                            </div>
                                            <div class="card-footer bg-light d-flex justify-content-between align-items-center">
                                                <button type="button" class="btn btn-outline-secondary btn-sm" id="clear_signature_btn">
                                                    <i class="fa fa-eraser mr-1"></i> Limpiar Firma
                                                </button>
                                                <span class="text-muted small">Firma en el espacio punteado</span>
                                            </div>
                                        </div>
                                        <div class="text-center mt-4">
                                            <button type="submit" class="btn btn-primary btn-lg px-5 font-weight-bold" id="submit_sign_btn">
                                                <i class="fa fa-check-circle mr-2"></i> Firmar y Enviar Convenio
                                            </button>
                                        </div>
                                    </form>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <script type="text/javascript">
                document.addEventListener("DOMContentLoaded", function () {
                    var canvas = document.getElementById("signature_canvas");
                    if (!canvas) return;
                    var ctx = canvas.getContext("2d");
                    var drawing = false;
                    var hasSigned = false;
                    ctx.lineWidth = 2.5;
                    ctx.lineCap = "round";
                    ctx.strokeStyle = "#000080";
                    function getPos(e) {
                        var rect = canvas.getBoundingClientRect();
                        var clientX = e.touches ? e.touches[0].clientX : e.clientX;
                        var clientY = e.touches ? e.touches[0].clientY : e.clientY;
                        return {x: clientX - rect.left, y: clientY - rect.top};
                    }
                    function startDrawing(e) {
                        drawing = true;
                        hasSigned = true;
                        var pos = getPos(e);
                        ctx.beginPath();
                        ctx.moveTo(pos.x, pos.y);
                    }
                    function draw(e) {
                        if (!drawing) return;
                        e.preventDefault();
                        var pos = getPos(e);
                        ctx.lineTo(pos.x, pos.y);
                        ctx.stroke();
                    }
                    function stopDrawing() {
                        drawing = false;
                    }
                    canvas.addEventListener("mousedown", startDrawing);
                    canvas.addEventListener("mousemove", draw);
                    canvas.addEventListener("mouseup", stopDrawing);
                    canvas.addEventListener("mouseleave", stopDrawing);
                    canvas.addEventListener("touchstart", startDrawing, {passive: false});
                    canvas.addEventListener("touchmove", draw, {passive: false});
                    canvas.addEventListener("touchend", stopDrawing);
                    document.getElementById("clear_signature_btn").addEventListener("click", function () {
                        ctx.clearRect(0, 0, canvas.width, canvas.height);
                        hasSigned = false;
                    });
                    document.getElementById("agreement_sign_form").addEventListener("submit", function (e) {
                        if (!hasSigned) {
                            alert("Por favor, firme en el recuadro antes de enviar el convenio.");
                            e.preventDefault();
                            return false;
                        }
                        document.getElementById("signature_data_input").value = canvas.toDataURL("image/png");
                    });
                });
            </script>
        </t>
    </template>
</odoo>
